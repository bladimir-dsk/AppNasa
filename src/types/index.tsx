
//acemos el typado de un componente para tener mas claridad de los datos que nos van a retornar
export type PostImage = {
    date?: string,
    explanation?: string,
    hdurl?: string, 
    media_type?: string,
    service_version?: string,
    title?: string,
    url?: string
}